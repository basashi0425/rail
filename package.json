{
  "name": "captcha-cloak",
  "version": "1.0.0",
  "description": "A web proxy using Playwright and Firefox",
  "main": "index.js",
  "scripts": {
    "install": "playwright install --with-deps firefox",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18"
  }
}
