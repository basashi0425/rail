{
  "name": "captcha-cloak",
  "version": "1.0.0",
  "description": "A private web proxy with CAPTCHA evasion using Playwright.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install --with-deps firefox"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.41.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
